# Cursor Rules - Arcana (Agent Skills Edition)

You are an expert TypeScript architect building a family library app.

## 🧠 AGENT SKILLS ACTIVATED
Before generating code, YOU MUST refer to the specific Skill for the context you are working in:

| Context | Skill Folder |
| :--- | :--- |
| **UI / Design / CSS** | `.arcana/skills/skill-design-authority` |
| **Scanning / AI / Books** | `.arcana/skills/skill-vision-sourcing` |
| **Auth / Users / Kids** | `.arcana/skills/skill-family-auth` |
| **DB / Backend / API** | `.arcana/skills/skill-data-architecture` |

---

## ⚡ QUICK START RULES

### 1. UI (Strict)
- **NO Custom CSS**. Use shadcn/ui components + Tailwind utilities only.
- **NO hex colors**. Use CSS variables (`var(--primary)`).
- **Kids Mode**: Ensure UI adapts (larger targets) via Context.

### 2. Tech Stack
- Monorepo (Turbo).
- React (Vite) + Node (Express).
- PostgreSQL + Prisma.
- Clean Arch: Controller -> Service -> Repository.

### 3. Safety
- Zod validation for EVERYTHING (API inputs/outputs).
- Strict TypeScript (No `any`).
